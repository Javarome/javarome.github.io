export class Link {
  /**
   *
   * @param {string} name
   * @param {URL} link
   * @param description
   */
  constructor(name, link, description) {
    this.name = name
    this.url = link
    this.description = description
  }
}
